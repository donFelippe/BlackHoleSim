3D simulation of a black hole 
To run in  performance mode use cargo run --release

For improving performance lower WIDTH and HEIGHT in main.
The simulation allows rotation of the black hole, due to it being computation-heavy give it some time.
